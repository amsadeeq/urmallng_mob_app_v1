// lib/shared/styles/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF6200EE); // Example primary color
  static const Color primaryDark = Color(0xFF3700B3);
  // Darker shade for contrast
  static const Color accent = Color(0xFF03DAC5); // Accent color
  static const Color background = Color(0xFFFFFFFF); // White background
  static const Color textPrimary = Color(
    0xFF212121,
  ); // Dark text for readability
  static const Color textSecondary = Color(
    0xFF757575,
  ); // Lighter text for secondary info
  static const Color error = Color(0xFFB00020); // Error color
  static const Color success = Color(0xFF4CAF50); // Success color
  static const Color primaryColor = Color.fromRGBO(228, 0, 122, 1);
  static const Color white = Colors.white;
  static const Color oFFwhite = Color.fromARGB(255, 254, 243, 243);
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
  static const Color successGreen = Color.fromARGB(190, 1, 121, 5);
}
